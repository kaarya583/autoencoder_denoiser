# Model artifacts

The current selected INT8 model is [final_model.bin](../final_model.bin) at the repository root. Its checkpoint, calibration, metrics and hash verification are in [the champion directory](../output/esp32/champions/gtcrn_qat_20260915/).

The original larger U-Net checkpoint archive is preserved at [legacy/robust_UNet_model.zip](legacy/robust_UNet_model.zip). These binary files require Git LFS.
